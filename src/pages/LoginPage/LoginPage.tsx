const LoginPage = (): React.ReactElement => {
  return (
    <>
      <img src="./img/logo.svg" alt="Beacher logo" width={90} height={90} />
      <h1 className="app-title">Beacher</h1>
    </>
  );
};

export default LoginPage;
